当前这轮轨迹优化已完成的部分：

1. 已在 `TrackNoiseFilter` 中加入低速场景下的锐角折返过滤，用连续三个点的夹角、小范围桥接距离和时间窗口来识别“星芒状乱窜”漂移。
2. 已在 `TrackPathSanitizer` 中加入 Douglas-Peucker 抽稀，用于历史轨迹和按时间排序的清洗链路，减少锯齿和冗余点，同时保留原始过滤后段落的距离统计。
3. 已用有状态卡尔曼滤波替换 `AdaptiveTrackSmoother` 之前的 EMA 平滑，并在后台服务的会话恢复、开始与结束节点上补齐了 `seed/reset` 生命周期管理。
4. 已在后台服务中加入轻量驻留模式：当低速点在小范围内持续停留一段时间后，会将尾部漂移点坍缩为单个驻留中心，并暂停继续追加驻留范围内的新点。

当前剩余更重型、可后续评估的方案：

1. 如果目标场景偏驾车或公路骑行，可后续评估接入地图绑路（Map Matching / HMM）能力。
