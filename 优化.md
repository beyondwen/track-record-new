当前这轮优化项已处理完成。

本轮已完成：
1. `DashboardUiController` 不再在高频 `render()` 中重复执行 `TrackPathSanitizer.sanitize(...)`，直接复用跟踪层已维护好的 `session.totalDistanceKm`。
2. `HistoryController` 对总距离和总时长做了缓存，避免切换 Tab、导入导出等轻状态变化时重复遍历整份历史列表。
3. `DashboardUiController` 将高频 `String.format` 改成了轻量字符串拼接，降低定时刷新带来的额外 GC 压力。
